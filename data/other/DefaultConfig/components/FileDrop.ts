export type CfgType = {
  DisplayCount: boolean;
  DisplayTips: boolean;
  BackdropBlur: boolean;
}

const FileDropCfg = {
  DisplayCount: true,
  DisplayTips: true,
  BackdropBlur: true,
}

export default FileDropCfg